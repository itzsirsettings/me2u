node_modules
.next
out
build
coverage
*.tsbuildinfo
.next/types
server/dist
# Kilo worktrees are separate checkouts — never format them from the root.
.kilo
# Playwright artifacts
playwright-report
test-results
# Standalone SQL snapshots (hand-maintained, not app code)
COMPLETE_MIGRATION.sql
RAILWAY_MIGRATION.sql
# Numbered migrations are hand-maintained SQL too; prettier has no SQL parser,
# and check-changed-format passes paths explicitly, so it would error on them.
railway/migrations/*.sql
